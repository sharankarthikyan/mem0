# Dependency ranges are bounded on BOTH sides for reproducible rebuilds. The
# upper bounds cap the next major (the version known to work today is noted in
# the comment where it differs from the floor). Regenerate a hashed lockfile
# (e.g. `pip-compile` / `uv pip compile`) before release for full reproducibility.
fastapi>=0.68.0,<1.0
uvicorn>=0.15.0,<1.0
# Pinned to the 2.0 line: the codebase uses the legacy 1.x-style Query API
# (db.query(...)), which 2.0 still supports, but an unbounded floor let a rebuild
# straddle 1.4 and 2.0 nondeterministically. Tested on 2.0.x.
sqlalchemy>=2.0,<2.1
python-dotenv>=0.19.0,<2.0
alembic>=1.7.0,<2.0
psycopg2-binary>=2.9.0,<3.0
python-multipart>=0.0.27,<1.0
urllib3>=2.7.0,<3.0
fastapi-pagination>=0.12.0,<1.0
# Pinned to the 2.x line (tested on 2.0.7); an unbounded `>=0.1.92` floor let a
# rebuild resolve anything from 0.1.x to the latest major.
mem0ai>=2.0,<3.0
openai>=1.40.0,<3.0
mcp[cli]>=1.25.4,<2.0
starlette>=0.40.0,<2.0
# Pydantic v2 is required (the code uses v2 models); pin it explicitly rather
# than relying on a transitive resolution.
pydantic>=2.0,<3.0
pytest>=7.0.0,<9.0
pytest-asyncio>=0.21.0,<1.0
httpx>=0.24.0,<1.0
pytest-cov>=4.0.0,<8.0
tenacity==9.1.2
anthropic==0.51.0
ollama==0.4.8
# Self-hosted reranking (local ONNX cross-encoder, no torch) AND BM25 sparse
# vectors for hybrid search. mem0 lists fastembed as optional, so pin it here.
fastembed>=0.3.1,<1.0
# Optional: shared search cache backend (used only when OPENMEMORY_REDIS_URL is set).
redis>=5.0.0,<9.0
# Opt-in external reranker (RERANKER_PROVIDER=cohere): `pip install cohere`.
# Not installed by default — the default reranker is the self-hosted FastEmbed one.
